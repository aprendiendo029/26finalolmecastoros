import { defineConfig } from 'vite'

export default defineConfig({
  // Reemplaza 'tu-repositorio' por el nombre EXACTO de tu repositorio en GitHub
  base: '/26finalolmecastoros/', 
  build: {
    outDir: 'dist',
  }
})